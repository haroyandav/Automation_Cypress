let val = 'Monday'

if(typeof val === 'string' && val.length >= 6){
    
    console.log(` 1---Given value: ${val} is a string`);
}else{
    console.log(` 1---Given value: ${val} is NOT a string`);
}
