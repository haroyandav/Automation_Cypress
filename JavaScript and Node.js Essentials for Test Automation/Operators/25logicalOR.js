let day = 'Monday'

if (day === 'Saturday' || day === 'Sunday'){
    
    console.log(`Given day--1: ${day} is a Weekend`);
} else {
    
    console.log(`Given day--1: ${day} is NOT a Weekend`);
}

let day1 = 'Saturday'

if (day1 === 'Saturday' || day1 === 'Sunday'){
    
    console.log(`Given day--2: ${day1} is a Weekend`);
} else {
    
    console.log(`Given day--2: ${day1} is NOT a Weekend`);
}