let count = 0
let isDataReturned = false

while (isDataReturned === false) {
    
    console.log(count);
    
    if(count == 10) {
        isDataReturned = true
    
    }
    count = count + 1
    
}